/** Default invite link validity (days). */
export const INVITE_DEFAULT_EXPIRY_DAYS = 7

export const INVITE_EXPIRY_OPTIONS = [7, 14, 30] as const

export const INVITE_MAX_EXPIRY_DAYS = 30

export const INVITE_PATH_PREFIX = '/invite'